# Local secrets (never commit)
.local/
.env
.env.*

# Local build artifacts (never commit)
.builds/

# Saved API reference exports (internal reading material — do not commit), anywhere
Full Public Booking API Documentation*

